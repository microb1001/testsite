<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{.Title}}</title>
    <style>
        body {
            font-family: sans-serif;
        }
        h1 {
            background: #ddd;
        }
        #sidebar {
            float: left;
        }
    </style>
</head>
<body>
<h1>{{.Title}}</h1>

<div id="sidebar">
    {{block "sidebar" .}}
    <h2>Links</h2>
    {{/* The dashes in the following template directives
    ensure the generated HTML of this list contains no
    extraneous spaces or line breaks. */}}
    <a href="/" class=""><span class="button__text">Назад</span>
        {{- range .Pager}}
        {{if .Current}}
        <a class="{{.Class}}"><span class="button__text">{{.Page}}</span></a>
        {{else}}
        <a href="/{{.Url}}" class="{{.Class}}"><span class="button__text">{{.Page}}</span></a>
        {{end}}
        {{- end}}
    <a href="/" class=""><span class="button__text">Вперед</span>
    <ul>
        {{- range .Links}}
        <li><a href="{{.URL}}">{{.Brief}}</a></li>
        {{- end}}
    </ul>
    {{end}}
</div>

{{block "content" .}}
<div id="content">
    {{.Body}}
</div>
{{end}}
</body>
</html>